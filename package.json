{
  "name": "workspace",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "npm install -g @ionic/cli @capacitor/cli",
    "build": "cd .. && npx ionic capacitor build android --prod"
  },
  "dependencies": {
    "three": "^0.181.1"
  },
  "devDependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
