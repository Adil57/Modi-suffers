{
  "name": "workspace",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": { // <--- Is block ke andar
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "npm install -g @ionic/cli @capacitor/cli", // <--- Nayi Line 1
    "build": "ionic capacitor build android --prod"      // <--- Nayi Line 2
  },
  "dependencies": {
    "three": "^0.181.1"
  },
// ... baaki file waise hi rahegi ...
}
